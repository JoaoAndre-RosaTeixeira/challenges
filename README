use command "python main.py" to run this code

Before run this programme in terminal enter :
    - pip install inquirer
    - pip install simpledbf
    - pip install pandas
    - pip install openpyxl
    - pip install matplotlib
    - pip install numpy

use arrow up and down to navigate in terminal and press enter to validate. for next read the instructions on terminal
